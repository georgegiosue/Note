<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Note" parent="Theme.MaterialComponents.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/PrimaryColor</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/WhiteColor</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/PrimaryColor</item>

        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>

        <item name="android:windowBackground">@color/BFDColor</item>

    </style>

    <style name="ItemsBase" parent="Theme.MaterialComponents">
        <item name="android:textColor">@color/WhiteColor</item>
        <item name="colorPrimary">@color/OrangeMaterial</item>
        <item name="colorSurface">@color/SecondColorDark</item>
        <item name="android:background">@color/SecondColorDark</item>
    </style>

    <style name="BottomNavViewActive" parent="BottomNavView">
        <item name="android:textSize">15sp</item>
    </style>

    <style name="BottomNavView" parent="Theme.MaterialComponents">
        <item name="bottomNavigationStyle">@style/Widget.MaterialComponents.BottomNavigationView.Colored</item>
        <item name="colorPrimary">@color/PrimaryColor</item>
        <item name="colorOnPrimary">@color/WhiteColor</item>
        <item name="itemIconTint">@null</item>
    </style>

</resources>