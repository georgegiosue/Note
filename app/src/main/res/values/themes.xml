<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Note" parent="Theme.MaterialComponents.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/WhiteColor</item>


        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/SecondColor</item>


        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/PrimaryColor</item>
        <!-- Customize your theme here. -->

        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>


    </style>

    <style name="ButtonNavViewItems">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/WhiteColor</item>
    </style>

</resources>